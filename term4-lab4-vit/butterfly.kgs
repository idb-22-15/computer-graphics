<?xml version="1.0" encoding="utf-8"?>
<project vert=" varying vec2 texCoord;&#xD; varying vec3 Normal;&#xD; &#xD; void main(void)&#xD; {&#xD;    gl_Position =  gl_ProjectionMatrix * gl_ModelViewMatrix*(gl_Vertex);&#xD;    texCoord = gl_MultiTexCoord0.xy;&#xD;    Normal = normalize(gl_Normal);&#xD; }" pix=" varying vec2 texCoord;&#xD; varying vec3 Normal;&#xD; uniform sampler2D iTexture0;&#xD; &#xD; &#xD; vec2 rotate(vec2 p, float theta) {&#xD;    float s = sin(theta);&#xD;    float c = cos(theta);&#xD;    mat2 rotationMatrix = mat2(c, -s, s, c);&#xD;    return rotationMatrix * p;&#xD;}&#xD;&#xD; &#xD; void main(void)&#xD; {&#xD; &#x9;float x = texCoord.x - 0.7;&#xD; &#x9;float y = texCoord.y;&#xD; &#x9;&#xD; &#x9;vec2 rot1 = rotate(vec2(x, y), radians(40.));&#xD; &#x9;vec2 rot2 = rotate(vec2(x, y), radians(80.));&#xD; &#x9;vec2 rot3 = rotate(vec2(x, y), radians(-70.));&#xD;;&#xD; &#x9;&#xD; &#x9;float fx1 = rot1.x*rot1.x * 7;&#xD; &#x9;float fx2 = rot2.x*rot2.x * 10;&#xD; &#x9;float fx3 = rot3.x*rot3.x * 1000;&#xD; &#x9;&#xD; &#x9;float round1 = (x*1.)*(x*1.) + (y*2.5)*(y*2.5);&#xD; &#x9;float rad1 = 0.05;&#xD; &#x9;float rx = 0.3;&#xD; &#x9;float ry = -0.2;&#xD; &#x9;float round2 = (x*1.+rx)*(x*1.+rx) + ((y+ry)*1.5)*((y+ry)*1.5);&#xD; &#x9;float rad2 = 0.1;&#xD; &#x9;&#xD; &#x9;if (y &gt; fx1) gl_FragColor = vec4(texture2D(iTexture0,texCoord).rgb,1.0);&#xD; &#x9;if (y &gt; fx2 &amp;&amp; round2 &lt; rad2) gl_FragColor = vec4(texture2D(iTexture0,texCoord).rgb,1.0);&#xD; &#x9;if (y &gt; fx3) gl_FragColor = vec4(texture2D(iTexture0,texCoord).rgb,1.0);&#xD; &#x9;&#xD; &#x9;if (round1  &lt; rad1) gl_FragColor = vec4(texture2D(iTexture0,texCoord).rgb,1.0);&#xD; }&#xD; " tt0="1" t0="D:\art\uni\projects\computer-graphics\term4-lab4-vit\texture.jpg" tt1="0" t1="" tt2="0" t2="" tt3="0" t3="" />